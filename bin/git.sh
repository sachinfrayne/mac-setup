#!/usr/bin/env bash

git config --global user.email "sachin.frayne@gmail.com"
git config --global user.name "sachin-frayne"
git config --global credential.helper osxkeychain
git config --global pull.rebase true
